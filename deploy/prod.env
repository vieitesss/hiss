# Hiss — Environment config (committed, non-secret)
# DB password is injected at deploy time — never committed.
COMPOSE_PROJECT_NAME=hiss-prod
IMAGE=ghcr.io/vieitesss/hiss
IMAGE_TAG=0.1.0
APP_VERSION=0.1.0
APP_PORT=8003
FEATURE_LABEL_FILTERING=false
POSTGRES_DB=hiss
POSTGRES_USER=postgres
